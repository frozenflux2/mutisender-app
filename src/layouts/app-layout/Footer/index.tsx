import React from "react";

// styled component
import { CopyrightView } from "views/layout";

// -----------------------------------------------------

const Footer = React.memo(CopyrightView);
export default Footer;
